# FaviconRotate.js
## Usage
```javascript
new FaviconRotate([
      'favicons/html5.ico',
      'favicons/js.ico',
      'favicons/rails.ico'
    ], 1000);
```
Where `1000` is `timeout`. Default `timeout` is `1500`
